pipeline:
  name: MLOps Credit Card Approval
  identifier: MLOps_Credit_Card_Approval
  orgIdentifier: default
  projectIdentifier: HarnessMLOPS
  description: "End-to-end MLOps pipeline using AWS native services: SageMaker Experiments for tracking, SageMaker Model Registry for versioning and approval, S3 for artifact storage, EKS for serving. Includes automated quality and fairness gates, canary deployment, and Dynatrace continuous verification."
  tags:
    domain: mlops
    app: credit-card-approval
    cloud: aws
  properties:
    ci:
      codebase:
        repoName: HarnessMLOpsDemo
        build: <+input>
  variables:
    - name: imageTag
      type: String
      value: <+pipeline.sequenceId>
      description: Docker image tag for this pipeline run
    - name: awsRegion
      type: String
      value: us-east-1
      description: AWS region for SageMaker and S3
    - name: s3BucketName
      type: String
      value: <+input>
      description: S3 bucket for model artifacts and metrics
    - name: modelPackageGroup
      type: String
      value: credit-card-approval
      description: SageMaker Model Registry package group name
    - name: apiImageName
      type: String
      value: credit-card-api
      description: Flask prediction API image name
    - name: appImageName
      type: String
      value: credit-card-app
      description: Next.js frontend image name
  stages:
    - stage:
        name: Train Model
        identifier: Train_Model
        type: CI
        spec:
          cloneCodebase: true
          infrastructure:
            type: KubernetesDirect
            spec:
              connectorRef: <+input>
              namespace: mlops
              os: Linux
          execution:
            steps:
              - step:
                  type: Run
                  name: Train and Register Model
                  identifier: Train_and_Register_Model
                  spec:
                    connectorRef: account.Dockerhub
                    image: python:3.12-slim
                    shell: Sh
                    command: |-
                      pip install --quiet -r requirements.txt sagemaker boto3
                      python scripts/train_model.py \
                        --n_estimators 100 \
                        --model_name <+pipeline.variables.modelPackageGroup>
                      export RUN_NAME=$(cat outputs/run_name.txt)
                      export MODEL_PACKAGE_ARN=$(cat outputs/model_package_arn.txt)
                      export S3_METRICS_URI=$(cat outputs/s3_metrics_uri.txt)
                      echo "RUN_NAME=$RUN_NAME"
                      echo "MODEL_PACKAGE_ARN=$MODEL_PACKAGE_ARN"
                      echo "S3_METRICS_URI=$S3_METRICS_URI"
                    envVariables:
                      AWS_DEFAULT_REGION: <+pipeline.variables.awsRegion>
                      S3_BUCKET: <+pipeline.variables.s3BucketName>
                      SAGEMAKER_MODEL_PACKAGE_GROUP: <+pipeline.variables.modelPackageGroup>
                    outputVariables:
                      - name: RUN_NAME
                        value: RUN_NAME
                        type: String
                      - name: MODEL_PACKAGE_ARN
                        value: MODEL_PACKAGE_ARN
                        type: String
                      - name: S3_METRICS_URI
                        value: S3_METRICS_URI
                        type: String
    - stage:
        name: Evaluate Model
        identifier: Evaluate_Model
        type: CI
        spec:
          cloneCodebase: true
          infrastructure:
            type: KubernetesDirect
            spec:
              connectorRef: <+input>
              namespace: mlops
              os: Linux
          execution:
            steps:
              - step:
                  type: Run
                  name: Quality and Fairness Gate
                  identifier: Quality_and_Fairness_Gate
                  spec:
                    connectorRef: account.Dockerhub
                    image: python:3.12-slim
                    shell: Sh
                    command: |-
                      pip install --quiet boto3
                      python scripts/evaluate_model.py \
                        --s3_metrics_uri <+stages.Train_Model.spec.execution.steps.Train_and_Register_Model.output.outputVariables.S3_METRICS_URI> \
                        --region <+pipeline.variables.awsRegion>
                    envVariables:
                      AWS_DEFAULT_REGION: <+pipeline.variables.awsRegion>
    - stage:
        name: Build and Push Images
        identifier: Build_and_Push_Images
        type: CI
        spec:
          cloneCodebase: true
          infrastructure:
            type: KubernetesDirect
            spec:
              connectorRef: <+input>
              namespace: mlops
              os: Linux
          execution:
            steps:
              - step:
                  type: BuildAndPushDockerRegistry
                  name: Build Flask API
                  identifier: Build_Flask_API
                  spec:
                    connectorRef: <+input>
                    repo: <+input>/<+pipeline.variables.apiImageName>
                    tags:
                      - <+pipeline.variables.imageTag>
                      - latest
                    dockerfile: deployment/api/Dockerfile
                    context: deployment/api
                    caching: true
              - step:
                  type: BuildAndPushDockerRegistry
                  name: Build Next.js Frontend
                  identifier: Build_Nextjs_Frontend
                  spec:
                    connectorRef: <+input>
                    repo: <+input>/<+pipeline.variables.appImageName>
                    tags:
                      - <+pipeline.variables.imageTag>
                      - latest
                    dockerfile: deployment/app/harness_bank_app/Dockerfile
                    context: deployment/app/harness_bank_app
                    buildArgs:
                      NEXT_PUBLIC_API_URL: http://credit-card-api-staging:5000
                    caching: true
    - stage:
        name: Deploy Staging
        identifier: Deploy_Staging
        type: Deployment
        spec:
          deploymentType: Kubernetes
          service:
            serviceRef: <+input>
            serviceInputs: <+input>
          environment:
            environmentRef: staging
            deployToAll: false
            infrastructureDefinitions:
              - identifier: staging
          execution:
            steps:
              - step:
                  type: K8sRollingDeploy
                  name: Rolling Deploy to Staging
                  identifier: Rolling_Deploy_Staging
                  spec:
                    skipDryRun: false
                    pruningEnabled: false
                  timeout: 10m
            rollbackSteps:
              - step:
                  type: K8sRollingRollback
                  name: Rollback Staging
                  identifier: Rollback_Staging
                  spec:
                    pruningEnabled: false
                  timeout: 10m
        failureStrategies:
          - onFailure:
              errors:
                - AllErrors
              action:
                type: StageRollback
        delegateSelectors:
          - <+input>
        tags: {}
    - stage:
        name: Integration Tests
        identifier: Integration_Tests
        type: CI
        spec:
          cloneCodebase: false
          infrastructure:
            type: KubernetesDirect
            spec:
              connectorRef: <+input>
              namespace: mlops
              os: Linux
          execution:
            steps:
              - step:
                  type: Run
                  name: Health Check
                  identifier: Health_Check
                  spec:
                    connectorRef: account.Dockerhub
                    image: curlimages/curl:latest
                    shell: Sh
                    command: |-
                      curl -f http://credit-card-api-staging:5000/health
                      echo "Health check passed"
              - step:
                  type: Run
                  name: Prediction Smoke Tests
                  identifier: Prediction_Smoke_Tests
                  spec:
                    connectorRef: account.Dockerhub
                    image: python:3.12-slim
                    shell: Sh
                    command: |-
                      pip install --quiet requests

                      python - <<'EOF'
                      import requests, sys

                      BASE = "http://credit-card-api-staging:5000"

                      # High-income, owns car and house → expect Approved (1)
                      r = requests.post(f"{BASE}/predict", json={
                          "income": 150000, "children": 0, "ownCar": True, "ownHouse": True
                      })
                      assert r.status_code == 200, f"Unexpected status: {r.status_code}"
                      result = r.json()
                      assert result["prediction"] == 1, \
                          f"Expected Approved for high-income applicant, got: {result}"
                      print(f"[PASS] High-income applicant: {result['result_str']}")

                      # Low-income, no assets → expect Rejected (0)
                      r = requests.post(f"{BASE}/predict", json={
                          "income": 30000, "children": 4, "ownCar": False, "ownHouse": False
                      })
                      assert r.status_code == 200, f"Unexpected status: {r.status_code}"
                      result = r.json()
                      assert result["prediction"] == 0, \
                          f"Expected Rejected for low-income applicant, got: {result}"
                      print(f"[PASS] Low-income applicant: {result['result_str']}")

                      print("\nAll integration tests passed.")
                      EOF
    - stage:
        name: Approve Production Deployment
        identifier: Approve_Production_Deployment
        type: Approval
        spec:
          execution:
            steps:
              - step:
                  type: HarnessApproval
                  name: Approve for Production
                  identifier: Approve_for_Production
                  spec:
                    approvalMessage: |-
                      Staging tests have passed. Review before approving production.

                      SageMaker Experiments run: <+stages.Train_Model.spec.execution.steps.Train_and_Register_Model.output.outputVariables.RUN_NAME>
                      SageMaker Model Registry ARN: <+stages.Train_Model.spec.execution.steps.Train_and_Register_Model.output.outputVariables.MODEL_PACKAGE_ARN>
                      S3 metrics: <+stages.Train_Model.spec.execution.steps.Train_and_Register_Model.output.outputVariables.S3_METRICS_URI>
                      Image tag: <+pipeline.variables.imageTag>

                      Approving will:
                        1. Set model to Approved in SageMaker Model Registry
                        2. Trigger canary rollout to production EKS
                        3. Flask API pods will load the newly approved model on startup
                    includePipelineExecutionHistory: true
                    approvers:
                      minimumCount: 1
                      disallowPipelineExecutor: false
                      userGroups:
                        - account._account_all_users
                  timeout: 24h
    - stage:
        name: Approve Model in Registry
        identifier: Approve_Model_in_Registry
        type: CI
        spec:
          cloneCodebase: true
          infrastructure:
            type: KubernetesDirect
            spec:
              connectorRef: <+input>
              namespace: mlops
              os: Linux
          execution:
            steps:
              - step:
                  type: Run
                  name: Approve Model Package
                  identifier: Approve_Model_Package
                  spec:
                    connectorRef: account.Dockerhub
                    image: python:3.12-slim
                    shell: Sh
                    command: |-
                      pip install --quiet boto3
                      python scripts/promote_model.py \
                        --model_package_arn <+stages.Train_Model.spec.execution.steps.Train_and_Register_Model.output.outputVariables.MODEL_PACKAGE_ARN> \
                        --region <+pipeline.variables.awsRegion>
                    envVariables:
                      AWS_DEFAULT_REGION: <+pipeline.variables.awsRegion>
    - stage:
        name: Deploy Production
        identifier: Deploy_Production
        type: Deployment
        spec:
          deploymentType: Kubernetes
          service:
            serviceRef: <+input>
            serviceInputs: <+input>
          environment:
            environmentRef: production
            deployToAll: false
            infrastructureDefinitions:
              - identifier: prodns
          execution:
            steps:
              - stepGroup:
                  name: Canary Rollout
                  identifier: Canary_Rollout
                  steps:
                    - step:
                        type: K8sCanaryDeploy
                        name: Deploy Canary 25pct
                        identifier: Deploy_25_Canary
                        spec:
                          skipDryRun: false
                          instanceSelection:
                            type: Percentage
                            spec:
                              percentage: 25
                        timeout: 10m
                    - step:
                        type: Verify
                        name: Verify with Dynatrace
                        identifier: Verify_with_Dynatrace
                        spec:
                          isMultiServicesOrEnvs: false
                          type: Canary
                          monitoredService:
                            type: Default
                            spec: {}
                          spec:
                            sensitivity: MEDIUM
                            duration: 10m
                        timeout: 2h
                        failureStrategies:
                          - onFailure:
                              errors:
                                - Verification
                              action:
                                type: StageRollback
                          - onFailure:
                              errors:
                                - Unknown
                              action:
                                type: StageRollback
                    - step:
                        type: K8sCanaryDelete
                        name: Delete Canary Pods
                        identifier: Delete_Canary_Pods
                        spec: {}
                        timeout: 10m
              - stepGroup:
                  name: Full Rollout
                  identifier: Full_Rollout
                  steps:
                    - step:
                        type: K8sRollingDeploy
                        name: Rolling Deploy to Production
                        identifier: Rolling_Deploy_Production
                        spec:
                          skipDryRun: false
                          pruningEnabled: false
                        timeout: 10m
            rollbackSteps:
              - step:
                  type: K8sCanaryDelete
                  name: Delete Canary on Rollback
                  identifier: Delete_Canary_Rollback
                  spec: {}
                  timeout: 10m
              - step:
                  type: K8sRollingRollback
                  name: Rollback Production
                  identifier: Rollback_Production
                  spec:
                    pruningEnabled: false
                  timeout: 10m
        failureStrategies:
          - onFailure:
              errors:
                - AllErrors
              action:
                type: StageRollback
        delegateSelectors:
          - <+input>
